#include <stdio.h>
int main()
{
	int n[3], s[3], m[3]; // n = number, s = series (ABC), m = most (most to least)
	scanf("%d %d %d", &n[0], &n[1], &n[2]);
	scanf("%c %c %c", &s[0], &s[1], &s[2]);
	
	return 0;
}
