#include <stdio.h>
#include <string.h>
int main()
{
	int num[3], order[3], _order;
	for (int i = 0; i < 3; i++)
	{
		scanf("%d", &num[i]);
	}
	scanf("%s", _order);
	int *p = _order;
	for (int i = 0; i < 3; i++)
	{
		order[i] = &p
		p++;
	}
	return 0;
}
